[general]
bars = 10
framerate = 30
lower_cutoff_freq = 50
higher_cutoff_freq = 10000

[input]
method = pipewire
source = auto

[output]
method = raw
data_format = ascii
ascii_max_range = 7
bar_delimiter = 0
raw_target = /dev/stdout
